import { EmbeddingsApi } from "../api";
import { EmbeddingsModelSlug } from "../model/paramTypes";
import { GenerateEmbeddingsResponse } from "../model/returnTypes";
export declare class EmbeddingsModel {
    private readonly apiInstance;
    readonly slug: EmbeddingsModelSlug;
    readonly workspaceId: string;
    constructor({ apiInstance, slug, workspaceId, }: {
        apiInstance: EmbeddingsApi;
        slug: EmbeddingsModelSlug;
        workspaceId: string;
    });
    readonly generateEmbeddings: ({ inputs, }: {
        inputs: Array<{
            input: string;
        }>;
    }) => Promise<GenerateEmbeddingsResponse>;
}
//# sourceMappingURL=embeddingsModel.d.ts.map