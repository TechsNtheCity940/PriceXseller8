import { RAGApi } from "../api";
import { IFilesApiManager } from "../files/types";
import { AddFilesParams, RagChunker } from "./paramTypes";
import { RagFile } from "./returnTypes";
export declare class RagCollection {
    private readonly filesApiManager;
    private readonly ragApi;
    files: Array<RagFile>;
    readonly id: string;
    readonly name: string;
    readonly chunker: Required<RagChunker>;
    readonly workspaceId: string;
    constructor(params: {
        files: Array<RagFile>;
        filesApiManager: IFilesApiManager;
        id: string;
        name: string;
        chunker: Required<RagChunker>;
        ragApi: RAGApi;
        workspaceId: string;
    });
    readonly addFiles: ({ filepaths, }: AddFilesParams) => Promise<void>;
    readonly delete: ({}: {}) => Promise<void>;
}
//# sourceMappingURL=ragCollection.d.ts.map