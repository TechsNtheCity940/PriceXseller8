import { ModelsApi } from "../api";
import { CompleteParams } from "./paramTypes";
import { CompleteResponse } from "./returnTypes";
export declare abstract class Model {
    protected readonly apiInstance: ModelsApi;
    readonly id: string;
    readonly workspaceId: string;
    protected constructor({ apiInstance, id, workspaceId, }: {
        apiInstance: ModelsApi;
        id: string;
        workspaceId: string;
    });
    readonly complete: ({ autoTemplate, guidance, maxGeneratedTokenCount, query, rag, temperature, topK, topP, }: CompleteParams) => Promise<CompleteResponse>;
}
//# sourceMappingURL=abstractModel.d.ts.map